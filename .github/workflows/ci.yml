# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches:
      - main
      - 'release/*'

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

# Cancel any previous check runs for the same pull request to avoid redundant workflows.
concurrency:
  group: ${{ github.event_name == 'pull_request' && format('{0}-{1}', github.workflow, github.event.pull_request.number) || format('{0}-{1}', github.workflow, github.run_id) }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    name: Test Suite
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - stable
          - beta
          - nightly
        include:
          - rust: stable
            can-fail: false
          - rust: beta
            can-fail: false
          - rust: nightly
            can-fail: true
    continue-on-error: ${{ matrix.can-fail }}

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Check Helm CRD manifest sync
      if: matrix.rust == 'stable'
      run: cmp examples/crds.yaml helm/crds/modelexpress-crds.yaml

    - name: Install Rust toolchain
      run: |
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH

    - name: Check Rust version
      run: rustc --version

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target/
        key: ${{ runner.os }}-cargo-${{ matrix.rust }}-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-${{ matrix.rust }}-
          ${{ runner.os }}-cargo-

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y protobuf-compiler

    - name: Check formatting
      if: matrix.rust == 'stable'
      run: cargo fmt --all -- --check

    - name: Lint with clippy
      if: matrix.rust == 'stable'
      run: cargo clippy --all-targets --all-features -- -D warnings

    - name: Build debug
      run: cargo build --verbose

    - name: Run unit tests
      run: cargo test --verbose --lib

    - name: Run server memory-backend unit tests
      run: cargo test -p modelexpress-server --features memory-backend --lib

    - name: Run integration tests
      if: matrix.rust == 'stable'
      run: cargo test -p modelexpress-server --features integration-tests --test in_process_server

    - name: Build release
      run: cargo build --release --verbose

    - name: Run doc tests
      run: cargo test --doc

  helm-verify:
    name: Helm Chart Verify
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # Pinned: the get-helm-3 script resolves helm-latest-version, so an
    # unpinned install silently follows major releases.
    - name: Install Helm
      run: |
        curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 \
          | DESIRED_VERSION=v3.16.4 bash

    - name: Lint the chart against every shipped values file
      run: |
        helm lint helm/
        for f in helm/values-production.yaml helm/values-development.yaml \
                 helm/values-local-storage.yaml helm/test-values.yaml; do
          echo "== $f"
          helm lint helm/ -f "$f"
        done

    # The monitoring.coreos.com resources are capability-gated, so both states
    # have to be exercised. These assert on rendered CONTENT rather than exit
    # status: a gate stuck permanently false still exits 0, so a bare
    # `helm template > /dev/null` would pass forever without proving anything.
    - name: Render without the Prometheus Operator CRDs
      run: |
        helm template mx helm/ > /tmp/plain.yaml
        grep -q "kind: Deployment" /tmp/plain.yaml
        ! grep -q "monitoring.coreos.com" /tmp/plain.yaml

    - name: Reject Operator resources when the CRDs are absent
      run: |
        if helm template mx helm/ --set metrics.podMonitor.enabled=true 2>/dev/null; then
          echo "expected a failure: a PodMonitor was rendered with no CRD available"
          exit 1
        fi

    - name: Render with the Prometheus Operator CRDs
      run: |
        helm template mx helm/ \
          --api-versions monitoring.coreos.com/v1 \
          --set metrics.podMonitor.enabled=true \
          --set metrics.rules.enabled=true \
          --set metrics.rules.client=true \
          --set metrics.dashboard.enabled=true \
          --set metrics.clientPodMonitor.enabled=true \
          --set metrics.clientPodMonitor.selector.matchLabels.app=mx-vllm \
          > /tmp/operator.yaml
        grep -q "kind: PodMonitor" /tmp/operator.yaml
        grep -q "kind: PrometheusRule" /tmp/operator.yaml
        grep -q "mx-vllm" /tmp/operator.yaml
        grep -q "grafana_dashboard" /tmp/operator.yaml

    - name: Reject a client PodMonitor with no selector
      run: |
        if helm template mx helm/ \
             --api-versions monitoring.coreos.com/v1 \
             --set metrics.clientPodMonitor.enabled=true 2>/dev/null; then
          echo "expected a failure: an empty selector matches every pod in the namespace"
          exit 1
        fi

    - name: Check the rendered alerting rules with promtool
      run: |
        PROM=prometheus-3.1.0.linux-amd64
        curl -sSL -o /tmp/prom.tar.gz \
          "https://github.com/prometheus/prometheus/releases/download/v3.1.0/${PROM}.tar.gz"
        tar -xzf /tmp/prom.tar.gz -C /tmp
        helm template mx helm/ \
          --api-versions monitoring.coreos.com/v1 \
          --set metrics.rules.enabled=true \
          --set metrics.rules.client=true \
          --show-only templates/prometheusrule.yaml \
          | python3 -c "import sys,yaml,json; print(yaml.safe_dump(yaml.safe_load(sys.stdin)['spec']))" \
          > /tmp/rules.yaml
        /tmp/${PROM}/promtool check rules /tmp/rules.yaml

  tls-feature-matrix:
    name: TLS Feature Matrix
    runs-on: ubuntu-latest
    strategy:
      matrix:
        features:
          - --no-default-features --features tls-rustls
          - --no-default-features --features openssl
          - --no-default-features --features tls-rustls,gcs
          - --no-default-features --features openssl,gcs

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Rust toolchain
      run: |
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target/
        key: ${{ runner.os }}-cargo-tls-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-tls-
          ${{ runner.os }}-cargo-

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y protobuf-compiler pkg-config libssl-dev

    - name: Lint with clippy
      run: cargo clippy --workspace --all-targets ${{ matrix.features }} -- -D warnings

    - name: Build
      run: cargo build --workspace ${{ matrix.features }} --verbose

  tls-no-backend-rejected:
    name: TLS No-Backend Rejected
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Rust toolchain
      run: |
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y protobuf-compiler

    - name: Building with no TLS backend must fail
      run: |
        if cargo check -p modelexpress-common --no-default-features 2>build.log; then
          echo "expected the build to be rejected when no TLS backend is selected"
          exit 1
        fi
        grep -q "no TLS backend selected" build.log

  python-test:
    name: Python Tests (protobuf ${{ matrix.protobuf_major }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - protobuf_major: 5
            protobuf_spec: "protobuf==5.27.2"
          - protobuf_major: 6
            protobuf_spec: "protobuf>=6.30.2,<7.0.0"

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install uv
      run: pip install uv

    - name: Install dependencies
      working-directory: modelexpress_client/python
      run: |
        uv venv .venv --python 3.12
        uv pip install -e ".[dev]" '${{ matrix.protobuf_spec }}'

    - name: Run tests
      working-directory: modelexpress_client/python
      run: |
        .venv/bin/python -m pytest tests/ -v

  go-test:
    name: Go Bindings
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: "1.24.x"
        cache-dependency-path: modelexpress_client/go/go.sum

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y protobuf-compiler

    - name: Install protoc plugins
      run: |
        go install google.golang.org/protobuf/cmd/protoc-gen-go@v1.36.11
        go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@v1.6.0
        echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

    - name: Validate generated bindings
      run: |
        modelexpress_client/go/generate_proto.sh
        if [ -n "$(git status --porcelain -- modelexpress_client/go)" ]; then
          git status --porcelain -- modelexpress_client/go
          git diff -- modelexpress_client/go
          exit 1
        fi

    - name: Run Go tests
      working-directory: modelexpress_client/go
      run: go test ./...

  artifact-transfer-contract:
    name: Artifact Transfer Contract
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - name: Checkout repository
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      with:
        persist-credentials: false

    - name: Install Rust toolchain
      run: |
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH

    - name: Cache dependencies
      uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target/
        key: ${{ runner.os }}-cargo-artifact-transfer-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-artifact-transfer-
          ${{ runner.os }}-cargo-

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y protobuf-compiler

    - name: Run artifact transfer contract tests
      run: |
        cargo test -p model-express-workspace-tests --test artifact_transfer_contract

  integration-test:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: test

    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Rust toolchain
      run: |
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH

    - name: Check Rust version
      run: rustc --version

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target/
        key: ${{ runner.os }}-cargo-stable-${{ hashFiles('**/Cargo.lock') }}

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y protobuf-compiler

    - name: Build release
      run: cargo build --release

    - name: Run registry backend Redis integration tests
      env:
        REDIS_URL: redis://localhost:6379
      run: |
        cargo test -p model-express-workspace-tests \
          --test registry_backend_redis \
          -- --include-ignored --test-threads=1

    - name: Run P2P Redis status ordering test
      env:
        MX_TEST_REDIS_URL: redis://localhost:6379
      run: |
        cargo test -p modelexpress-server --lib \
          p2p::backend::redis::tests::older_status_update_does_not_replace_newer_status \
          -- --ignored --exact --test-threads=1

    - name: Run Refit service Redis integration tests
      env:
        REDIS_URL: redis://localhost:6379
      run: |
        cargo test -p model-express-workspace-tests \
          --test refit_service_redis \
          -- --include-ignored --test-threads=1

    - name: Run integration test script
      env:
        MX_METADATA_BACKEND: redis
        REDIS_URL: redis://localhost:6379
      run: |
        if [ -f run_integration_tests.sh ]; then
          chmod +x run_integration_tests.sh
          ./run_integration_tests.sh || echo "Integration test script failed (expected in CI)"
        fi

  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Rust toolchain
      run: |
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH

    - name: Check Rust version
      run: rustc --version

    - name: Install cargo-audit
      run: cargo install cargo-audit

    - name: Run security audit
      run: cargo audit

    - name: Install and Run cargo-deny
      run: |
        cargo-deny --version || cargo install cargo-deny@0.16.4
        cargo-deny --no-default-features check --hide-inclusion-graph licenses bans --config ${{ github.workspace }}/deny.toml

  code-coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    needs: test

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Rust toolchain
      run: |
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH

    - name: Check Rust version
      run: rustc --version

    - name: Install LLVM tools
      run: |
        rustup component add llvm-tools-preview

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y protobuf-compiler

    - name: Install cargo-llvm-cov
      run: cargo install cargo-llvm-cov

    - name: Generate code coverage
      run: cargo llvm-cov --all-features --workspace --lcov --output-path lcov.info

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        files: lcov.info
        fail_ci_if_error: false

  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: test

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Build Docker image
      run: |
        docker build -f docker/Dockerfile -t model-express:latest .

    - name: Test Docker image
      run: |
        docker run --rm --detach --name test-server -p 8001:8001 model-express:latest
        sleep 10
        # Test that the server is responding (basic health check)
        timeout 30 bash -c 'until curl -s http://localhost:8001/health; do sleep 1; done' || echo "Health check failed (expected for gRPC)"
        docker stop test-server || true

  k8s-grpc-test:
    name: Kubernetes gRPC Transfer Test
    runs-on: ubuntu-latest
    needs: test

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install kubectl
      run: |
        curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
        chmod +x kubectl
        sudo mv kubectl /usr/local/bin/

    - name: Install Helm
      run: |
        curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

    - name: Run gRPC transfer K8s test
      run: |
        chmod +x test_grpc_transfer_k8s.sh
        ./test_grpc_transfer_k8s.sh
